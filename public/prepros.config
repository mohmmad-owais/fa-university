module.exports = {
  // Specify the folders to watch
  watch: [
    {
      // Define the public folder
      path: './public', // Path to the public folder
      // Enable access to all files in the public folder
      include: ['*'], // Includes all files
    },
    // You can add other folders to watch here
    {
      path: './src', // Example for source files
      include: ['*.js', '*.css'], // Include JavaScript and CSS files
    },
  ],
  // Other configuration options can go here
  server: {
    // If using a local server, specify the port
    port: 3000,
  },
};```
